@model Actor

@{
    ViewData["Title"] = "Delete Actor comfirmation";
}

<div class="row text">
    <div class="col-md-8 offset-2">
        <p>
            <h1>Delete comfirmation for @Model.FullName</h1>
        </p>
    </div>


    <div class="col-md-6 offset-3">

        <div class="form-group text-center">
            <img class="border-info rounded-circle" style="width:150px;height:150px;" id="ProfilePictureURLPreview" />
        </div>

        <div class="form-group">
            <label asp-for="ProfilePictureURL" class="control-label"></label>
            <input asp-for="ProfilePictureURL" class="form-control"  readonly/>
            <span asp-validation-for="ProfilePictureURL" class="text-danger"> </span>
        </div>

        <div class="form-group ">
            <label asp-for="FullName" class="control-label"></label>
            <input asp-for="FullName" class="form-control" readonly/>
            <span asp-validation-for="FullName" class="text-danger"> </span>
        </div>
        <div class="form-group">
            <label asp-for="Bio" class="control-label"></label>
            <input asp-for="Bio" class="form-control" readonly/>
            <span asp-validation-for="Bio" class="text-danger"> </span>
        </div>
        <br />
        <div class="form-group">
            <form asp-action="Delete" >
                <input type="hidden" asp-for="Id"/>
                <input type="submit" value="Comfirm" class="btn btn-danger float-end"/>
            </form>
            <a class="btn btn-outline-secondary  float-start" asp-action="Index">Show All</a>
        </div>

    </div>


</div>

@section Scripts{
    <script>
        $(document).ready(function () {
            var output = document.getElementById("ProfilePictureURLPreview");
            if (!($("#ProfilePictureURL").val()) == "") {
                output.src = $("#ProfilePictureURL").val();
            }

        })
        //$("#ProfilePictureURL").on("change", function () {
        //    var output = document.getElementById("ProfilePictureURLPreview");
        //    output.src = $(this).val();
        //})
    </script>
}